{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Welcome, to the Photographers Hub</h1>
    <p class="text-center">Here you will be able to find the most creative and skilled photographers.
        Want to become a part of this community? Go ahead and
        <a href="{{ path('app_register') }}">register</a>!</p>
    <p class="text-center">Looking for a photographer? Check out the list of our hub members
        <a href="{{ path('app_photographers') }}">here</a> or by navigating through the navigation bar!</p>

    <div class="mt-5 mb-4">
        <h4 class="text-center">Or you can filter photographers by location:</h4>
    </div>
    <div class="form-group row justify-content-center align-items-center">
        <form action="{{ path('app_photographers') }}" method="get" class="form-inline">
            <div class="mx-1">
                <input name="filter" type="text" class="form-control form-control-lg" placeholder="City/State/Country">
            </div>
            <div class="mx-1">
                <button type="submit" class="btn btn-primary btn-lg">Filter</button>
            </div>
        </form>
    </div>
{% endblock %}